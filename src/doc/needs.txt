frontmatter for info
template setup
pages for non-dynamic/non-list content
posts for dynamic/list/date content

published/not published variable in frontmatter
categories variable in frontmatter
tags variable in frontmatter
date variable in frontmatter (ISO Standard)

posts content will take date, and update file name to YYYY-MM-DD-{{Filename}}.ext

posts content will have all file data grouped into a sitedata.json file, passed to templates
includes post title
				url
				date
				tags
				categories
				published
				excerpt (content up to first paragraph)
				next post
				previous post

